name: Test

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

permissions:
  contents: read

jobs:
  test:
    uses: artkrsk/stub-package-workflows/.github/workflows/stub-test.yml@17a54677f1a78e137125ce050ff292a695782d0d # master
    with:
      stub_file: elementor-stubs.php
