name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  release:
    uses: artkrsk/stub-package-workflows/.github/workflows/stub-release.yml@17a54677f1a78e137125ce050ff292a695782d0d # master
    with:
      release_title: 'Elementor'
